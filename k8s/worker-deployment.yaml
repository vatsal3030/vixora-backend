apiVersion: apps/v1
kind: Deployment
metadata:
  name: vixora-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vixora-worker
  template:
    metadata:
      labels:
        app: vixora-worker
    spec:
      containers:
        - name: worker
          image: your-registry/vixora-backend:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: vixora-env
          env:
            - name: QUEUE_ENABLED
              value: "true"
            - name: RUN_WORKER
              value: "true"
            - name: RUN_SCHEDULER
              value: "false"
            - name: RUN_WORKER_ON_DEMAND
              value: "false"
